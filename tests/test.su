import common;

extern int main()
{
    version (OS.Windows)
    {
        puts("Windows OS");
    }
    else version (OS.Linux)
    {
        puts("Linux OS");
    }
    else
    {
        puts("Unknown OS!");
    }

    return 0;
}